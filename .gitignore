node_modules/
.DS_Store
Thumbs.db

# CSS compilado — gerado por `npm run build`.
# A Cloudflare roda o build no deploy, então não precisa ir para o git.
frontend/css/site.css

# Segredos: nunca versionar.
.env
.env.local
.env*.local
.vercel
